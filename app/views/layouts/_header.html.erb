<header class="flex items-center justify-around bg-pink-500 text-pink-100">
  <nav class="">
    <h1 class="text-4xl font-bold  text-white">✅Habitbi</h1>
  </nav>

  <div>
  <% if user_signed_in? %>
   <%= link_to "usename", "#" %>
  <%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete}  %>
  <% else %>
  <%= link_to "Sign up", new_user_registration_path %>
  <%= link_to "Login", new_user_session_path %>
  <% end %>


  </div>
</header>